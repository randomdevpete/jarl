import styled from "@emotion/styled";

/** Unbulleted, loosely spaced list used for the docs, API and demos indexes. */
export const LinkList = styled.ul`
  list-style: none;
  padding: 0;

  li {
    margin: 0.5rem 0;
  }
`;

export default LinkList;
